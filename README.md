# Electron Remote Desktop

This app uses [Create React App](https://github.com/facebook/create-react-app) and [Electron](https://www.electronjs.org/) to create a remote desktop application.

## Scaffolding

- new project with CRA
- install `concurrently wait-on cross-env electron electron-builder electronmon`
